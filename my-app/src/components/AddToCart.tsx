"use client";
import React from "react";

const ProductCard = () => {
  return (
    <>
      <button
        onClick={() => {
          console.log("clicked");
        }}
      >press</button>
    </>
  );
};

export default ProductCard;
